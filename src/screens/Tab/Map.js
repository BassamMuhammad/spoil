import {
  View,
  StyleSheet,
  Pressable,
  ScrollView,
  Image,
  Text,
} from 'react-native';
import React, {useState, useEffect} from 'react';

export const Map = () => {
  return <Text>Map</Text>;
};

const styles = StyleSheet.create({});
